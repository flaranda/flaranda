{
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "now-build": "next build && next export -o dist",
    "deploy": "now --scope flaranda --prod",
    "favicon": "favicon-emoji -e 🤓 -d ./assets/favicon.ico -p ./assets/favicon.png -m"
  },
  "engines": {
    "node": "10"
  },
  "dependencies": {
    "next": "^9.3.5",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "sass": "^1.26.3"
  },
  "devDependencies": {
    "favicon-emoji": "^2.3.0"
  }
}
