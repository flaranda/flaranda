{
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "now-build": "next build && next export -o dist",
    "deploy": "now --scope flaranda --target production",
    "favicon": "favicon-emoji -e 🤓 -d ./assets/favicon.ico -p ./assets/favicon.png -m"
  },
  "engines": {
    "node": "10"
  },
  "dependencies": {
    "animate-css-styled-components": "^1.0.2",
    "next": "^9.0.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "styled-components": "^4.3.2",
    "styled-reset": "^2.0.16",
    "webfontloader": "^1.6.28"
  },
  "devDependencies": {
    "babel-plugin-styled-components": "^1.10.6",
    "favicon-emoji": "^2.3.0"
  }
}
